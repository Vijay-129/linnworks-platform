---
title: <short problem name, e.g. "Retry a rate-limited Orders call">
added_by: <name>
date: <YYYY-MM-DD>
---

## Problem
What a macro dev is trying to do, in plain terms.

## Which API / helper
Which LinnworksAPI controller/method or which class in `LinnworksMacroHelpers`/`LinnMacroCustomer`
this touches. Link to `references/api/` or `references/macro/integrations/` rather than
re-explaining the endpoint here.

## Gotchas
Anything that isn't obvious from the code or the API docs — rate limits hit in practice,
ordering requirements, a Linnworks quirk, a config step that's easy to miss.

## Example
Minimal working C# snippet.
